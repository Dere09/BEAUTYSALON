<%- include('partials/header', { title: 'Registration Failed' , navActive: '' , hideMenu: true }) %>

  <div class="main-content">
    <style>
      .failure-container {
        max-width: 500px;
        margin: 80px auto;
        background: white;
        border-radius: 15px;
        padding: 40px 30px;
        text-align: center;
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
      }

      h1 {
        color: #dc3545;
        font-family: 'Playfair Display', serif;
        margin-bottom: 20px;
      }

      p {
        font-size: 18px;
        margin: 12px 0;
        color: #555;
      }

      .reason {
        color: #cc3366;
        font-weight: bold;
      }

      .try-again-btn {
        display: inline-block;
        margin-top: 25px;
        background-color: #cc3366;
        color: white;
        text-decoration: none;
        padding: 12px 30px;
        border-radius: 30px;
        font-weight: bold;
        transition: all 0.3s;
      }

      .try-again-btn:hover {
        background-color: #ad1457;
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(204, 51, 102, 0.3);
      }
    </style>

    <div class="failure-container">
      <h1>❌ Registration Failed</h1>
      <p>We're sorry, something went wrong.</p>
      <p class="reason">
        <%= reason %>
      </p>
      <a href="/register" class="try-again-btn">Try Again</a>
    </div>
  </div>

  <%- include('partials/footer') %>